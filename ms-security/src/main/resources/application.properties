spring.application.name=ms-security
server.port=8080

spring.mongodb.uri=mongodb+srv://juanalegria22503_db_user:1234@backend-juan.d02ubxc.mongodb.net/?appName=backend-juan
spring.mongodb.database=db_security

jwt.secret=aaa
jwt.expiration=3600000

# CAMBIO: configuración SMTP externalizada
# Ajusta estas variables en tu entorno o reemplázalas por valores reales.
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:juankr047@gmail.com}
spring.mail.password=${MAIL_PASSWORD:ffso zsdk gzxa pqqw}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS_ENABLE:true}

# CAMBIO: correo remitente
app.mail.from=${APP_MAIL_FROM:${MAIL_USERNAME:juankr047@gmail.com}}

# 2FA
app.2fa.code.expiration-ms=${APP_2FA_CODE_EXPIRATION_MS:300000}